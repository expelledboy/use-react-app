{
  "name": "use-react-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "husky": "^3.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn workspaces run precommit"
    }
  },
  "scripts": {
    "test": "yarn workspaces run test"
  }
}
