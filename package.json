{
  "name": "use-react-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "husky": "^3.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run lint"
    }
  },
  "scripts": {
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint"
  }
}
